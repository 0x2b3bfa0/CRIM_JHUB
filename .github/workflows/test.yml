on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: >
          pip install
          git+https://github.com/HCDigitalScholarship/intervals
          verovio
          nbmake
          pytest
      - run: pytest --nbmake *.ipynb
